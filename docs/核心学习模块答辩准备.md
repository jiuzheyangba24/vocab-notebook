# 核心学习模块答辩准备

> 本文档按**界面/功能**组织，每个界面标注前端、后端、数据库的实现位置和**具体代码行号**。

---

## 6.1.1 词库中心界面

### 功能描述
词库中心展示了系统内置的多个词库，包括初中词汇、高中词汇、四级核心词汇、六级核心词汇、考研核心词汇等。用户可以选择需要学习的词库进行导入。

### 前端实现

| 文件 | 位置 | 说明 |
|------|------|------|
| `frontend/src/views/LibraryView.vue` | 第 1-110 行 | 模板部分 |
| `frontend/src/views/LibraryView.vue` | 第 44-80 行 | 官方词库列表展示 |
| `frontend/src/views/LibraryView.vue` | 第 217-219 行 | `enterLibrary()` 进入词库 |
| `frontend/src/composables/useLibrary.js` | - | 词库配置和加载 |

```javascript
// 第 44-79 行：官方词库展示
<section class="library-section">
  <div class="section-header">
    <h2><LibraryBig class="section-icon" /> 官方词库</h2>
  </div>
  <div class="library-grid">
    <div 
      v-for="lib in libraries" 
      :key="lib.id"
      class="library-card"
      @click="enterLibrary(lib.id)"
    >
      <div class="card-icon">...</div>
      <div class="card-info">
        <h3>{{ lib.name }}</h3>
        <p class="word-count">{{ getActualWordCount(lib.id) }} 词</p>
      </div>
    </div>
  </div>
</section>

// 第 217-219 行：进入词库
function enterLibrary(libraryId) {
  router.push(`/study/${libraryId}`)
}
```

### 后端实现

**说明**: 词库数据存储在前端静态JSON文件中，不涉及后端API

### 数据库实现

**说明**: 词库数据不存储在数据库，用户进度保存在 LocalStorage

---

## 6.1.2 生词本 - 卡片视图

### 功能描述
生词本的卡片视图模式，以翻转卡片的形式展示单词，正面显示英文单词，点击可翻转查看中文释义。支持发音功能和删除操作。

### 前端实现

| 文件 | 位置 | 说明 |
|------|------|------|
| `frontend/src/views/VocabularyView.vue` | 第 30-79 行 | 卡片视图模板 |
| `frontend/src/views/VocabularyView.vue` | 第 32-58 行 | 翻转卡片组件 |
| `frontend/src/views/VocabularyView.vue` | 第 282-288 行 | `speakWord()` 发音功能 |
| `frontend/src/views/VocabularyView.vue` | 第 359-364 行 | `deleteCurrentWord()` 删除功能 |

```javascript
// 第 30-58 行：卡片视图模板
<div v-if="viewMode === 'card'" class="card-view">
  <div class="word-card" v-if="currentWord">
    <div class="card-inner" :class="{ flipped: showDetails }" @click="showDetails = !showDetails">
      <!-- 正面 -->
      <div class="card-front">
        <div class="word-text">{{ currentWord.headWord }}</div>
        <span class="word-phonetic">{{ currentWord.pronunciation }}</span>
        <button class="speak-btn" @click.stop="speakWord(currentWord.headWord)">🔊</button>
        <div class="hint">点击查看释义</div>
      </div>
      <!-- 背面 -->
      <div class="card-back">
        <div class="definition">{{ currentWord.definition }}</div>
      </div>
    </div>
  </div>
</div>

// 第 282-288 行：发音功能 (Web Speech API)
function speakWord(word) {
  if (!word) return
  const utterance = new SpeechSynthesisUtterance(word)
  utterance.lang = 'en-US'
  utterance.rate = 0.9
  speechSynthesis.speak(utterance)
}

// 第 359-364 行：删除功能
function deleteCurrentWord() {
  if (confirm('确定删除这个单词吗？')) {
    store.deleteWord()
    showDetails.value = false
  }
}
```

### 后端实现

| 文件 | 位置 | 说明 |
|------|------|------|
| `backend/src/main/java/com/vocab/controller/WordController.java` | 第 45-70 行 | `getWords()` 获取单词列表 |
| `backend/src/main/java/com/vocab/controller/WordController.java` | 第 110-122 行 | `deleteWord()` 删除单词 |

### 数据库实现

| 文件 | 位置 | 说明 |
|------|------|------|
| `database/schema.sql` | 第 36-60 行 | `words` 表 |

---

## 6.1.3 生词本 - 列表视图

### 功能描述
生词本的列表视图模式，以表格形式展示所有已添加的单词，包含单词、释义、发音按钮和删除按钮，便于快速浏览和管理。

### 前端实现

| 文件 | 位置 | 说明 |
|------|------|------|
| `frontend/src/views/VocabularyView.vue` | 第 82-108 行 | 列表视图模板 |
| `frontend/src/views/VocabularyView.vue` | 第 267-274 行 | `filteredWords` 搜索过滤 |
| `frontend/src/views/VocabularyView.vue` | 第 366-374 行 | `deleteWord()` 删除单词 |

```javascript
// 第 82-108 行：列表视图模板
<div v-else class="list-view">
  <div 
    v-for="word in filteredWords" 
    :key="word.id" 
    class="list-item"
    @click="selectWord(word)"
  >
    <div class="item-main">
      <div class="word">{{ word.headWord }}</div>
      <span class="phonetic">{{ word.pronunciation }}</span>
      <button class="speak-btn-mini" @click.stop="speakWord(word.headWord)">🔊</button>
    </div>
    <div class="item-def">{{ word.definition }}</div>
    <div class="item-mastery">
      <div class="mastery-bar">
        <div class="mastery-fill" :style="{ width: word.mastery_level + '%' }"></div>
      </div>
      <span>{{ word.mastery_level }}%</span>
    </div>
    <button class="delete-btn" @click.stop="deleteWord(word)">×</button>
  </div>
</div>

// 第 267-274 行：搜索过滤
const filteredWords = computed(() => {
  if (!searchQuery.value.trim()) return store.vocabulary
  const query = searchQuery.value.toLowerCase()
  return store.vocabulary.filter(w => 
    w.headWord.toLowerCase().includes(query) ||
    w.definition.toLowerCase().includes(query)
  )
})
```

### 后端实现

**同 6.1.2** - `GET /api/words` 获取单词列表

### 数据库实现

**同 6.1.2** - `words` 表

---

## 6.1.4 添加单词界面

### 功能描述
单词添加弹窗界面，用户输入单词后可自动获取释义、发音和例句。支持手动编辑和自定义。

### 前端实现

| 文件 | 位置 | 说明 |
|------|------|------|
| `frontend/src/views/VocabularyView.vue` | 第 111-134 行 | 添加弹窗模板 |
| `frontend/src/views/VocabularyView.vue` | 第 321-336 行 | `addWord()` 添加单词 |
| `frontend/src/composables/useApi.js` | - | `fetchWordInfo()` 自动获取释义 |

```javascript
// 第 111-133 行：添加弹窗模板
<Teleport to="body">
  <div v-if="showAddModal" class="modal-overlay">
    <div class="modal-card">
      <div class="modal-header">
        <h3>添加单词</h3>
        <button class="close-btn" @click="showAddModal = false">×</button>
      </div>
      <div class="modal-body">
        <input 
          v-model="newWord" 
          type="text" 
          placeholder="输入单词..."
          @keypress.enter="addWord"
        >
        <button class="submit-btn" @click="addWord" :disabled="isAdding">
          {{ isAdding ? '添加中...' : '添加单词' }}
        </button>
        <button class="batch-btn" @click="showBatchModal = true">批量添加</button>
      </div>
    </div>
  </div>
</Teleport>

// 第 321-336 行：添加单词逻辑
async function addWord() {
  const word = newWord.value.trim()
  if (!word) return
  
  isAdding.value = true
  try {
    // 自动获取释义、发音、例句
    const wordInfo = await fetchWordInfo(word)
    // 保存到词库
    await store.addWord(wordInfo)
    newWord.value = ''
    showAddModal.value = false
  } catch (error) {
    alert(error.message)
  } finally {
    isAdding.value = false
  }
}
```

### 后端实现

| 文件 | 位置 | 说明 |
|------|------|------|
| `backend/src/main/java/com/vocab/controller/WordController.java` | 第 75-105 行 | `addWord()` 添加单词 |

```java
// 第 75-93 行
@PostMapping  // POST /api/words
public ResponseEntity<?> addWord(@RequestBody AddWordRequest request) {
    // 检查单词是否已存在
    if (wordRepository.findByUserIdAndWordAndIsDeletedFalse(userId, request.getHeadWord())
            .isPresent()) {
        return ResponseEntity.badRequest().body(Map.of("error", "该单词已存在"));
    }
    
    Word word = new Word();
    word.setWord(request.getHeadWord());
    word.setTranslation(request.getDefinition());
    word = wordRepository.save(word);
    
    return ResponseEntity.ok(result);
}
```

### 数据库实现

| 文件 | 位置 | 说明 |
|------|------|------|
| `database/schema.sql` | 第 36-60 行 | `words` 表 INSERT 操作 |

---

## 6.1.5 批量添加单词界面

### 功能描述
批量添加单词界面，用户可以一次性输入多个单词（每行一个），系统会自动查询并添加到生词本中。

### 前端实现

| 文件 | 位置 | 说明 |
|------|------|------|
| `frontend/src/components/BatchAddModal.vue` | 第 1-57 行 | 批量添加模板 |
| `frontend/src/components/BatchAddModal.vue` | 第 99-170 行 | `startBatchAdd()` 批量添加逻辑 |
| `frontend/src/views/VocabularyView.vue` | 第 137-156 行 | 简易批量添加 |
| `frontend/src/views/VocabularyView.vue` | 第 338-357 行 | `addBatchWords()` 批量添加 |

```javascript
// BatchAddModal.vue 第 1-28 行：批量添加模板
<template>
  <Modal v-model="showModal" title="批量添加单词">
    <p class="batch-tip">每行输入一个单词，系统将自动获取释义和例句</p>
    
    <textarea 
      v-model="inputText"
      placeholder="请输入单词，每行一个，例如：
hello
world
vocabulary"
      rows="10"
      :disabled="isAdding"
    ></textarea>
    
    <div class="batch-controls">
      <button @click="startBatchAdd" :disabled="isAdding || !inputText.trim()">
        {{ isAdding ? '添加中...' : '开始添加' }}
      </button>
    </div>
    
    <!-- 进度显示 -->
    <div v-if="isAdding" class="batch-progress">
      <div class="progress-bar">
        <div class="progress-fill" :style="{ width: progressPercent + '%' }"></div>
      </div>
      <p>正在添加: {{ currentIndex + 1 }}/{{ totalWords }} - {{ currentWord }}</p>
    </div>
  </Modal>
</template>

// 第 99-170 行：批量添加逻辑
async function startBatchAdd() {
  // 解析单词（每行一个）
  const words = inputText.value.split('\n')
    .map(w => w.trim())
    .filter(w => w.length > 0 && /^[a-zA-Z\s-]+$/.test(w))
  
  // 去重
  const uniqueWords = [...new Set(words.map(w => w.toLowerCase()))]
  
  // 逐个添加
  for (let i = 0; i < uniqueWords.length; i++) {
    const word = uniqueWords[i]
    currentWord.value = word
    
    // 检查是否已存在
    const exists = store.vocabulary.some(
      w => w.headWord.toLowerCase() === word.toLowerCase()
    )
    if (exists) {
      addResult(`${word} - 已存在，跳过`, 'skip')
      continue
    }
    
    // 获取单词信息并添加
    const wordInfo = await fetchWordInfo(word)
    await store.addWord(wordInfo)
    addResult(`${word} - 添加成功`, 'success')
  }
}
```

### 后端实现

**同 6.1.4** - 批量调用 `POST /api/words`

### 数据库实现

**同 6.1.4** - 批量 INSERT 到 `words` 表

---

## 6.1.6 学习页面

### 功能描述
词库学习页面，展示选中词库的单词列表，支持隐藏释义进行自测，点击显示按钮可查看答案，实现高效的记忆学习。

### 前端实现

| 文件 | 位置 | 说明 |
|------|------|------|
| `frontend/src/views/StudyView.vue` | 第 1-195 行 | 模板部分 |
| `frontend/src/views/StudyView.vue` | 第 55-110 行 | 单词列表展示 |
| `frontend/src/views/StudyView.vue` | 第 246-259 行 | 分页计算 |
| `frontend/src/views/StudyView.vue` | 第 319-334 行 | `speakWord()` 发音功能 |
| `frontend/src/views/StudyView.vue` | 第 336-344 行 | `markAsMastered()` 标记掌握 |
| `frontend/src/views/StudyView.vue` | 第 421-452 行 | `addAllToVocab()` 导入词库 |

```javascript
// 第 55-110 行：单词列表展示
<div class="word-list">
  <div 
    v-for="(word, index) in displayWords" 
    :key="word.headWord"
    class="word-row"
  >
    <span class="col-num">{{ (currentPage - 1) * pageSize + index + 1 }}</span>
    
    <div class="col-word">
      <span class="word-text" v-if="showWord">{{ word.headWord }}</span>
      <span class="word-text hidden" v-else>••••••</span>
      <button class="speak-btn" @click="speakWord(word.headWord)">🔊</button>
    </div>
    
    <div class="col-definition">
      <span v-if="showDefinition">{{ word.definition }}</span>
      <span v-else class="hidden">点击释义显示</span>
    </div>
    
    <div class="col-action">
      <button 
        v-if="!masteredSet.has(word.headWord)"
        @click="markAsMastered(word)"
      >移除</button>
      <button v-else @click="unmarkMastered(word)">恢复</button>
    </div>
  </div>
</div>

// 第 246-259 行：分页计算
const pageSize = computed(() => settings.dailyWords || 30)
const totalPages = computed(() => Math.ceil(words.value.length / pageSize.value))
const displayWords = computed(() => {
  let list = [...words.value]
  if (settings.wordOrder === 'random') {
    list = shuffleWithSeed(list, libraryId.value)
  }
  const start = (currentPage.value - 1) * pageSize.value
  return list.slice(start, start + pageSize.value)
})

// 第 336-339 行：标记已掌握
function markAsMastered(word) {
  masteredSet.value.add(word.headWord)
  saveMasteredList()  // 保存到 LocalStorage
}

// 第 421-452 行：全部导入到我的词库
async function addAllToVocab() {
  const existingWords = new Set(store.vocabulary.map(w => w.headWord.toLowerCase()))
  const newWords = words.value.filter(w => !existingWords.has(w.headWord.toLowerCase()))
  
  await store.importVocabulary(newWords)
  alert(`成功导入 ${newWords.length} 个新单词！`)
}
```

### 后端实现

**说明**: 学习浏览功能主要从静态词库加载，导入功能调用后端

| 文件 | 位置 | 说明 |
|------|------|------|
| `backend/src/main/java/com/vocab/controller/WordController.java` | 第 75-105 行 | `addWord()` 导入单词 |

### 数据库实现

| 文件 | 位置 | 说明 |
|------|------|------|
| `database/schema.sql` | 第 36-60 行 | `words` 表 |

---

## 文件位置汇总表

| 界面 | 前端文件 | 前端行号 | 后端文件 | 后端行号 |
|------|---------|---------|---------|---------|
| 6.1.1 词库中心 | `LibraryView.vue` | 44-80, 217-219 | - | - |
| 6.1.2 卡片视图 | `VocabularyView.vue` | 30-79, 282-288 | `WordController.java` | 45-70 |
| 6.1.3 列表视图 | `VocabularyView.vue` | 82-108, 267-274 | `WordController.java` | 45-70 |
| 6.1.4 添加单词 | `VocabularyView.vue` | 111-134, 321-336 | `WordController.java` | 75-105 |
| 6.1.5 批量添加 | `BatchAddModal.vue` | 1-57, 99-170 | `WordController.java` | 75-105 |
| 6.1.6 学习页面 | `StudyView.vue` | 55-110, 246-259, 421-452 | `WordController.java` | 75-105 |

---

## 常见答辩问题

1. **问：卡片翻转效果是怎么实现的？**
   - 答：`VocabularyView.vue` 第 33 行使用 CSS 3D 变换，`card-inner` 添加 `flipped` 类后触发 `transform: rotateY(180deg)`，配合 `backface-visibility: hidden` 实现翻转效果。

2. **问：发音功能是怎么实现的？**
   - 答：`VocabularyView.vue` 第 282-288 行使用浏览器原生的 Web Speech API，创建 `SpeechSynthesisUtterance` 对象设置语言为 `en-US`，调用 `speechSynthesis.speak()` 播放。

3. **问：批量添加是怎么处理重复单词的？**
   - 答：`BatchAddModal.vue` 第 134-141 行，先用 `store.vocabulary.some()` 检查单词是否已存在，存在则跳过并显示"已存在"提示。

4. **问：学习页面的分页是怎么实现的？**
   - 答：`StudyView.vue` 第 246-259 行使用 `computed` 计算属性，`displayWords` 根据 `currentPage` 和 `pageSize` 使用数组 `slice()` 方法截取当前页的数据。
